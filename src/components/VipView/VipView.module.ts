import { makeStyles } from 'tss-react/mui'

const useStyles = makeStyles()((theme: any) => {
  return {
    link: {
      color: '#303030',
      textDecoration: 'none',
    },
  }
})

export default useStyles
